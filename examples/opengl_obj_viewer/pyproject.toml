[build-system]
requires = ["briefcase"]

[tool.briefcase]
project_name = "OpenGL Demo"
bundle = "org.beeware.toga.examples"
version = "0.0.1"
url = "https://beeware.org"
license = "BSD-3-Clause"
license-files = [
    "LICENSE",
]
author = "Tiberius Yak"
author_email = "tiberius@beeware.org"

formal_name = "An OpenGL Demo"
description = "A testing app"
requires = [
    "../../travertino",
    "../../core",
]

[tool.briefcase.app.obj-viewer]
sources = ["obj_viewer"]

[tool.briefcase.app.obj-viewer-mobile]
sources = ["obj_viewer", "obj_viewer_mobile"]

[tool.briefcase.app.obj-viewer-qt]
sources = ["obj_viewer", "obj_viewer_qt"]

[tool.briefcase.app.obj-viewer.macOS]
requires = [
    "../../cocoa",
    "std-nslog>=1.0.0",
    "PyOpenGL",
]

[tool.briefcase.app.obj-viewer.linux]
requires = [
    "../../gtk",
    "PyOpenGL",
]

[tool.briefcase.app.obj-viewer-qt.linux]
requires = [
    "../../qt",
    "PyOpenGL",
]

#[tool.briefcase.app.obj-viewer.windows]
#requires = [
#    "../../winforms",
#]

# Mobile deployments
[tool.briefcase.app.obj-viewer-mobile.iOS]
requires = [
    "../../iOS",
    "std-nslog>=1.0.0",
]

[tool.briefcase.app.obj-viewer-mobile.android]
requires = [
    "../../android",
]

base_theme = "Theme.MaterialComponents.Light.DarkActionBar"

build_gradle_dependencies = [
    "com.google.android.material:material:1.12.0",
]

# Web deployment
#[tool.briefcase.app.obj-viewer.web]
#requires = [
#    "../../web",
#]
#style_framework = "Shoelace v2.3"
