[tool.codespell]
skip = ".git,*.pdf,*.svg"
# the way to make case sensitive skips of words etc
ignore-regex = "\bNd\b"
ignore-words-list = "MapPin"

# The coverage settings in this file only control `coverage report`. `coverage run` and
# `coverage combine` are controlled by the pyproject.toml files in each package's
# subdirectory.
#
# In each subdirectory's pyproject.toml, we use both of the following settings:
#   * `source_pkgs`: detects the given packages no matter where they're imported from.
#   * `source`: detects all files in the given directory, including files that are
#      never imported by the tests.
#
# When running through `tox`, this will produce duplicate entries which must be merged
# with `coverage combine`.

[tool.coverage.run]
plugins = ["coverage_conditional_plugin"]
relative_files = true

[tool.coverage.report]
show_missing = true
skip_covered = true
skip_empty = true
precision = 1
exclude_lines = [
    "pragma: no cover",
    "@(abc\\.)?abstractmethod",
    "if TYPE_CHECKING:",
    "class .+?\\(Protocol.*\\):",
    "@overload",
]

[tool.coverage.coverage_conditional_plugin.rules]
# Additional testbed rules are configured in the testbed module
no-cover-if-missing-setuptools_scm = "not is_installed('setuptools_scm')"
no-cover-if-missing-PIL = "not is_installed('PIL')"
no-cover-if-PIL-installed = "is_installed('PIL')"
no-cover-if-lt-py312 = "sys_version_info < (3, 12) and os_environ.get('COVERAGE_EXCLUDE_PYTHON_VERSION') != 'disable'"
no-cover-if-gte-py312 = "sys_version_info > (3, 12) and os_environ.get('COVERAGE_EXCLUDE_PYTHON_VERSION') != 'disable'"
no-cover-if-lt-py311 = "sys_version_info < (3, 11) and os_environ.get('COVERAGE_EXCLUDE_PYTHON_VERSION') != 'disable'"
no-cover-if-gte-py311 = "sys_version_info > (3, 11) and os_environ.get('COVERAGE_EXCLUDE_PYTHON_VERSION') != 'disable'"

[tool.ruff]
exclude = [
    ".template",  # Ruff fails to run at all if allowed to lint the cookiecutter syntax
    "core/src/toga/__init__.pyi"
]

[tool.ruff.lint]
# In addition to the default rules, these additional rules will be used:
extend-select = [
    "E",      # pycodestyle
    "W",      # pycodestyle
    "F",      # pyflakes
    "UP",     # pyupgrade
    "B",      # flake8-bugbear
    "YTT",    # flake8-2020
    "ASYNC",  # flake8-async
    "C4",     # flake8-comprehensions
    "I",      # isort
    # The SIM rules are *very* opinionated, and don't necessarily make for better code.
    # They may be worth occasionally turning on just to see if something could actually
    # use improvement.
    # "SIM",    # flake8-simplify
]

[tool.ruff.lint.per-file-ignores]
# PEP8 module naming conventions aren't appropriate
"examples/customwidget/helloworld/src/hello_world_widget/iOS_hello_world.py" = ["N999"]
"iOS/src/toga_iOS/__init__.py" = ["N999"]
"iOS/src/toga_iOS/hardware/__init__.py" = ["N999"]
"iOS/src/toga_iOS/resources/__init__.py" = ["N999"]
"iOS/src/toga_iOS/widgets/__init__.py" = ["N999"]
# Ignores needed to pass the Ruff 0.16 update.
"android/src/toga_android/app.py" = ["RUF012"]
"android/src/toga_android/icons.py" = ["RUF012"]
"android/src/toga_android/screens.py" = ["RUF012"]
"android/src/toga_android/widgets/dateinput.py" = ["DTZ012"]
"android/tests_backend/dialogs.py" = ["BLE001"]
"android/tests_backend/widgets/dateinput.py" = ["DTZ012"]
"cocoa/src/toga_cocoa/command.py" = ["RUF012"]
"cocoa/src/toga_cocoa/constraints.py" = ["SIM102"]
"cocoa/src/toga_cocoa/icons.py" = ["RUF012"]
"cocoa/src/toga_cocoa/screens.py" = ["RUF012"]
"cocoa/src/toga_cocoa/widgets/internal/refresh.py" = ["PLW0642"]
"cocoa/src/toga_cocoa/widgets/textinput.py" = ["PLW0211"]
"cocoa/tests_backend/window.py" = ["BLE001"]
"core/src/toga/app.py" = ["BLE001", "RUF012"]
"core/src/toga/documents.py" = ["SIM102"]
"core/src/toga/handlers.py" = ["BLE001"]
"core/src/toga/sources/base.py" = ["PLC0105"]
"core/src/toga/style/pack.py" = ["RUF009"]
"core/src/toga/widgets/base.py" = ["SIM102"]
"core/src/toga/widgets/canvas/state.py" = ["RUF009"]
"core/src/toga/widgets/dateinput.py" = [
    "DTZ011",
    # The `if` form is retained deliberately: converting to `self.value =
    # max(self.value, min)` unconditionally invokes the `value` setter (and
    # its `on_change` side effect) even when no clipping is required.
    "PLR1730",
]
"core/src/toga/widgets/timeinput.py" = [
    "DTZ005",
    # See the comment on the equivalent dateinput.py ignore above.
    "PLR1730",
]
"core/src/toga/window.py" = ["PLC0105"]
"core/tests/app/test_document_app.py" = ["RUF012"]
"core/tests/command/test_command.py" = ["PLR0124"]
"core/tests/command/test_group.py" = ["PLR0124"]
"core/tests/style/pack/test_aliases.py" = ["PT010"]
"core/tests/style/pack/test_deprecated_properties.py" = ["PT031"]
"core/tests/test_documents.py" = ["RUF012"]
"core/tests/test_handlers.py" = ["TRY002"]
"core/tests/test_platform.py" = ["PT031"]
"core/tests/widgets/canvas/test_deprecations.py" = ["E501", "SIM117"]
"core/tests/widgets/canvas/test_state_objects.py" = ["SIM117"]
"core/tests/widgets/test_dateinput.py" = ["DTZ001"]
"core/tests/widgets/test_table.py" = ["SIM117"]
"core/tests/widgets/test_timeinput.py" = ["DTZ001"]
"core/tests/widgets/test_tree.py" = ["SIM117"]
"core/tests/window/test_window.py" = ["BLE001"]
"demo/toga_demo/app.py" = ["EXE002"]
"dummy/src/toga_dummy/icons.py" = ["RUF012"]
"dummy/src/toga_dummy/plugins/image_formats.py" = ["TRY002"]
"dummy/src/toga_dummy/screens.py" = ["RUF012"]
"dummy/src/toga_dummy/utils.py" = ["RUF012", "TRY004"]
"dummy/src/toga_dummy/widgets/dateinput.py" = ["DTZ011"]
"dummy/src/toga_dummy/widgets/timeinput.py" = ["DTZ005"]
"examples/beeliza/beeliza/bot.py" = ["RUF012"]
"examples/colors/colors/app.py" = ["PLR1704"]
"examples/documentapp/documentapp/app.py" = ["RUF012"]
"examples/examples_overview/examples_overview/app.py" = ["PLW1510"]
"examples/screenshot/screenshot/app.py" = ["E501"]
"examples/selection/selection/app.py" = ["RUF012"]
"examples/table_columns/table_columns/app.py" = ["DTZ001", "DTZ011"]
"examples/tree_source/tree_source/app.py" = ["DTZ006"]
"examples/tutorial4/tutorial/app.py" = ["SIM117"]
"examples/webview/webview/app.py" = ["BLE001"]
"examples/window/window/app.py" = ["DTZ005"]
"gtk/src/toga_gtk/icons.py" = ["RUF012"]
"gtk/src/toga_gtk/screens.py" = ["RUF012"]
"gtk/src/toga_gtk/widgets/mapview.py" = ["BLE001"]
"gtk/src/toga_gtk/widgets/webview.py" = ["BLE001"]
"gtk/tests_backend/dialogs.py" = ["BLE001", "DTZ005"]
"gtk/tests_backend/window.py" = ["SIM102"]
"iOS/src/toga_iOS/constraints.py" = ["SIM102"]
"iOS/src/toga_iOS/icons.py" = ["RUF012"]
"iOS/src/toga_iOS/libs/uikit.py" = ["PIE796"]
"iOS/src/toga_iOS/screens.py" = ["RUF012"]
"iOS/src/toga_iOS/widgets/canvas.py" = ["RUF009"]
"iOS/src/toga_iOS/widgets/detailedlist.py" = ["RUF012"]
"iOS/tests_backend/dialogs.py" = ["BLE001"]
"iOS/tests_backend/widgets/scrollcontainer.py" = ["RUF018"]
"qt/src/toga_qt/icons.py" = ["RUF012"]
"qt/src/toga_qt/screens.py" = ["E501", "RUF012"]
"qt/src/toga_qt/widgets/table.py" = ["SIM102"]
"qt/src/toga_qt/widgets/tree.py" = ["SIM102"]
"qt/tests_backend/dialogs.py" = ["BLE001"]
"qt/tests_backend/widgets/tree.py" = ["PLR1704"]
"testbed/src/testbed/app.py" = ["RUF012"]
"testbed/tests/testbed.py" = ["BLE001"]
"testbed/tests/widgets/canvas/test_canvas.py" = ["SIM117"]
"testbed/tests/widgets/canvas/test_deprecated_code.py" = ["SIM117"]
"testbed/tests/widgets/test_dateinput.py" = ["DTZ005"]
"testbed/tests/widgets/test_optioncontainer.py" = ["PT031"]
"testbed/tests/window/test_window.py" = ["BLE001", "E501"]
"textual/src/toga_textual/icons.py" = ["RUF012"]
"textual/src/toga_textual/screens.py" = ["RUF012"]
"textual/tests_backend/probe.py" = ["BLE001"]
"web/src/toga_web/icons.py" = ["RUF012"]
"web/src/toga_web/screens.py" = ["RUF012"]
"web/src/toga_web/widgets/dateinput.py" = ["BLE001", "DTZ011"]
"web/src/toga_web/widgets/timeinput.py" = ["BLE001", "DTZ005"]
"winforms/src/toga_winforms/app.py" = ["BLE001"]
"winforms/src/toga_winforms/icons.py" = ["RUF012"]
"winforms/src/toga_winforms/keys.py" = ["PLR1704", "PLW0127"]
"winforms/src/toga_winforms/libs/proactor.py" = ["BLE001"]
"winforms/src/toga_winforms/libs/win32structures.py" = ["RUF012"]
"winforms/src/toga_winforms/screens.py" = ["RUF012"]
"winforms/src/toga_winforms/widgets/activityindicator.py" = ["RUF012"]
"winforms/src/toga_winforms/widgets/detailedlist.py" = ["SIM102"]
"winforms/tests_backend/dialogs.py" = ["BLE001"]
"winforms/tests_backend/widgets/detailedlist.py" = ["TRY002"]

[tool.ruff.lint.isort]
combine-as-imports = true
known-third-party = [
    "android",  # isort defaults to making this first-party because it can't be imported.
    "textual",  # We don't ever import textual, so Ruff doesn't know about it.
    "travertino", # In this repo, but still a separate package
]
known-first-party = [
    "testbed",
    "toga",
    "toga_android",
    "toga_cocoa",
    "toga_dummy",
    "toga_gtk",
    "toga_iOS",
    "toga_textual",
    "toga_web",
    "toga_winforms",
]

[tool.rumdl]
flavor = "mkdocs"
include = ["**/*.md"]
exclude = ["changes/template.md", "comment.md", "CODE_OF_CONDUCT.md", "docs/en/SUMMARY.md"]
respect-gitignore = true
force-exclude = true

# Disable rules:
# MD014: Forces commands in codeblocks to show output
# MD042: Empty link - buggy, still flagging on valid MkDocs formatting
# MD052: Reference link not found - buggy, flagging on valis MkDocs links
disable = ["MD014", "MD042", "MD052"]

[tool.rumdl.per-file-ignores]
"changes/**/*.md" = ["MD041"]
"docs/en/reference/api/**/*.md" = ["MD041"]
"docs/en/snippets/**/*.md" = ["MD041"]

[tool.rumdl.MD007]
indent = 4

[tool.rumdl.MD013]  # Line length
line_length = 999999  # Force reflow to paragraph content to remove linebreaks
reflow = true
reflow_mode = "normalize"

[tool.rumdl.MD026]  # Remove punctuation at the end of headings
punctuation = ",;:"

[tool.rumdl.MD033]  # No inline HTML
allowed_elements = ["nospell",]  # pyspelling inline disable tag

[tool.rumdl.MD046]
style = "fenced"

[tool.towncrier]
directory = "changes"
package = "toga"
package_dir = "core/src"
filename = "docs/en/about/releases.md"
title_format = "## {version} ({project_date})"
issue_format = "[#{issue}](https://github.com/beeware/toga/issues/{issue})"
template = "changes/template.md"
type = [
    { directory = "feature", name = "Features", showcontent = true },
    { directory = "bugfix", name = "Bugfixes", showcontent = true },
    { directory = "removal", name = "Backward Incompatible Changes", showcontent = true },
    { directory = "doc", name = "Documentation", showcontent = true },
    { directory = "misc", name = "Misc", showcontent = false },
]

[tool.setuptools_scm]
# We're not doing anything Python-related at the root level of the repo, but if this
# declaration isn't here, tox commands run from the root directory raise a warning that
# pyproject.toml doesn't contain a setuptools_scm section.

[dependency-groups]
# Extras used by developers *of* Toga are pinned to specific versions to
# ensure environment consistency.
pre-commit = [
    "pre-commit == 4.6.0",
]

setuptools-scm = [
    "setuptools_scm == 10.2.0",
]

towncrier = [
    "towncrier==25.8.0",
]

tox-uv = [
    "tox-uv == 1.35.2",
]

dev = [
    {include-group = "pre-commit"},
    {include-group = "tox-uv"},
    {include-group = "setuptools-scm"}
]

# Docs are always built on a specific Python version; see RTD and tox config files,
# and the docs contribution guide.
docs = [
    "beeware-docs-tools @ git+https://github.com/beeware/beeware-docs-tools",
    # "beeware-docs-tools @ file://../beeware-docs-tools",
    "tabulate==0.10.0",
    "mkdocs-redirects==1.2.3",
    "mkdocs-exclude==1.0.2",
]
