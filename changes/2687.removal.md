The `get_platform_factory` function and backend `factory` modules are deprecated. Widget authors should use `get_factory` instead and writers of new backends should use entry points to declare the implemented objects.  The new factory objects are not modules but instead are lazy namespace objects.
