On macOS, `app.paths.config` is now a `Config` folder inside the app's `~/Library/Application Support` folder, rather than a folder in `~/Library/Preferences`. Apple's guidelines reserve the Preferences folder for the system preferences API; apps should not create files there themselves.
