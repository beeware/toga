`toga.widgets.canvas.DrawingObject` has been renamed from `DrawingObject` to `DrawingAction`; the previous name still works, but will raise a `DeprecationWarning`. This is an internal class, and is unlikely to be used directly.
