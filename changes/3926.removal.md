If an app explicitly provides an app ID, the app name will be derived from the last part of an explicitly-provided App ID, rather than being implicitly derived from the module name. If an app name is explicitly provided, that will take precedence over a name derived from the app ID.
