If your app name contains underscores, or an explicitly provided app ID differs from your app module name, the app name will now be in [normalized form](https://packaging.python.org/en/latest/specifications/name-normalization). The most common impact of this change will be a change in the default icon name for your app (becoming `resources/my-app` instead of `resources/my_app`).
