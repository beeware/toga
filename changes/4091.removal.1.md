Using `headings` as a keyword argument when creating `Table` and `Tree` objects is deprecated; any `headings` argument should be replaced with `columns`. The use of `headings=None` to hide the header row has been deprecated; use `show_headings=False` instead.
