Specifying `accessors` when creating `Table` and `Tree` widgets is deprecated. Use of `accessor` arguments on `insert_column()` and `append_column()` methods has also been deprecated. Use `accessor` arguments on column objects or data sources instead.
