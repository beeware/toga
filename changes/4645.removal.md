The GTK, Qt, WinForms and Textual backends now use platformdirs to determine app-specific paths. System path configuration (such as XDG environment variables) is now honored; on Windows, this changes the location of the config, data, cache and log folders.
