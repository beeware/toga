The app name is now derived from the last part of an explicitly-provided App ID, rather than being implicitly derived from the module name. If the app name *is* determined from the module name, it will be [normalized](https://packaging.python.org/en/latest/specifications/name-normalization).
