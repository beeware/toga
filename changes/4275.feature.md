On iOS 18-, system effects are applied to the title bar when a scroll container is the content of the window and reaches the titlebar area; on iOS 26+, system effects are consistently applied to the title bar when a scroll container simply reaches the top of the window container layout.
