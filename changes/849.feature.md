Added a ``Frame`` widget: a container that visually groups its content using the platform's native grouping idiom (``NSBox`` on macOS, ``GtkFrame`` on GTK, a group box on Windows, a ``MaterialCardView`` on Android), with an optional title.
