When nesting stroke contexts or fill contexts for canvas drawing operations, leaving an argument (e.g. `color`) blank on an inner context now respects any value set on the outer context, instead of resetting it to the default.
