``BaseStyle.apply()`` (and therefore ``Pack.apply()``) no longer accepts multiple arguments. If you want to apply multiple arguments at once, apply them within the ``with style_object.batch_apply()`` context manager.
