["Core application components"]
path = ""

App.description = "The top-level representation of an application."
App.display = "table"
App.beta = ["web", "textual"]

Window.description = "An operating system-managed container of widgets."
Window.beta = ["web", "textual"]

MainWindow.description = "A window that can use the full set of window-level user interface elements."
MainWindow.beta = ["web", "textual"]

DocumentWindow.description = "A window that can be used as the main interface to a document-based app."
DocumentWindow.unsupported = ["iOS", "android", "web", "textual"]


["General widgets"]
path = "widgets"

ActivityIndicator.description = "A small animated indicator showing activity on a task of indeterminate length, usually rendered as a \"spinner\" animation."
ActivityIndicator.beta = ["web"]
ActivityIndicator.unsupported = ["textual"]

Button.description = "A button that can be pressed or clicked."
Button.beta = ["web", "textual"]

Canvas.description = "A drawing area for 2D vector graphics."
Canvas.unsupported = ["web", "textual"]

DateInput.description = "A widget to select a calendar date."
DateInput.beta = ["web"]
DateInput.unsupported = ["textual"]

DetailedList.description = "An ordered list of content where each item has an icon, a main heading, and a line of supplementary text."
DetailedList.beta = ["winforms"]
DetailedList.unsupported = ["web", "textual"]

Divider.description = "A separator used to visually distinguish two sections of content in a layout."
Divider.beta = ["web"]
Divider.unsupported = ["textual"]

ImageView.description = "A widget that displays an image."
ImageView.unsupported = ["web", "textual"]

Label.description = "A text label for annotating forms or interfaces."
Label.beta = ["web", "textual"]

MapView.description = "A zoomable map that can be annotated with location pins."
MapView.unsupported = ["web", "textual"]

MultilineTextInput.description = "A scrollable panel that allows for the display and editing of multiple lines of text."
MultilineTextInput.unsupported = ["web", "textual"]

NumberInput.description = "A text input that is limited to numeric input."
NumberInput.unsupported = ["web", "textual"]

PasswordInput.description = "A widget to allow the entry of a password. Any value typed by the user will be obscured, allowing the user to see the number of characters they have typed, but not the actual characters."
PasswordInput.beta = ["web"]
PasswordInput.unsupported = ["textual"]

ProgressBar.description = "A horizontal bar to visualize task progress. The task being monitored can be of known or indeterminate length."
ProgressBar.beta = ["web"]
ProgressBar.unsupported = ["textual"]

Selection.description = "A widget to select a single option from a list of alternatives."
Selection.beta = ["web"]
Selection.unsupported = ["textual"]

Slider.description = "A widget for selecting a value within a range. The range is shown as a horizontal line, and the selected value is shown as a draggable marker."
Slider.beta = ["web"]
Slider.unsupported = ["textual"]

Switch.description = "A clickable button with two stable states: True (on, checked); and False (off, unchecked). The button has a text label."
Switch.beta = ["web"]
Switch.unsupported = ["textual"]

Table.description = "A widget for displaying columns of tabular data."
Table.beta = ["android"]
Table.unsupported = ["iOS", "web", "textual"]

TextInput.description = "A widget for the display and editing of a single line of text."
TextInput.beta = ["web", "textual"]

TimeInput.description = "A widget to select a clock time."
TimeInput.beta = ["web"]
TextInput.unsupported = ["gtk", "textual"]

Tree.description = "A widget for displaying a hierarchical tree of tabular data."
Tree.unsupported = ["winforms", "iOS", "android", "web", "textual"]

WebView.description = "An embedded web browser."
WebView.unsupported = ["web", "textual"]

Widget.description = "The abstract base class of all widgets. This class should not be be instantiated directly."
Widget.display = "table"
Widget.beta = ["web", "textual"]


["Layout widgets"]
path = "containers"

Box.description = "A generic container for other widgets. Used to construct layouts."
Box.display = "table"
Box.beta = ["web", "textual"]

ScrollContainer.description = "A container that can display a layout larger than the area of the container."
ScrollContainer.beta = ["web"]
ScrollContainer.unsupported = ["textual"]

SplitContainer.description = "A container that divides an area into two panels with a movable border."
SplitContainer.unsupported = ["iOS", "android", "web", "textual"]

OptionContainer.description = "A container that can display multiple labeled tabs of content."
OptionContainer.unsupported = ["web", "textual"]


["Resources"]
path = "resources"

"App paths".description = "A mechanism for obtaining platform-appropriate filesystem locations for an application."
"App paths".path = "resources/app_paths.md"
"App paths".display = "table"
"App paths".beta = ["textual"]
"App paths".unsupported = ["web"]

Command.description = "A representation of app functionality that the user can invoke from menus or toolbars."
Command.display = "table"
Command.unsupported = ["iOS", "web", "textual"]

Dialogs.description = "Short-lived windows asking the user for input."
Dialogs.display = "table"
Dialogs.beta = ["web", "textual"]

Document.description = "A representation of a file on disk that will be displayed in one or more windows."
Document.display = "table"
Document.unsupported = ["iOS", "android", "web", "textual"]

Font.description = "A representation of a font."
Font.path = "resources/fonts.md"
Font.display = "table"
Font.unsupported = ["web", "textual"]

Icon.description = "A small, square image used to provide easily identifiable visual context to a widget."
Icon.path = "resources/icons.md"
Icon.display = "table"
Icon.beta = ["textual"]
Icon.unsupported = ["web"]

"Status icons".description = "Icons that appear in the system tray for representing app status while the app isn't visible."
"Status icons".unsupported = ["iOS", "android", "web", "textual"]

Image.description = "Graphical content of arbitrary size."
Image.path = "resources/images.md"
Image.display = "table"
Image.unsupported = ["web", "textual"]

Source.description = "A base class for data source implementations."
Source.path = "resources/sources/source.md"
Source.display = "hide"

ValueSource.description = "A data source describing a single value."
ValueSource.display = "hide"
ValueSource.path = "resources/sources/value_source.md"

ListSource.description = "A data source describing an ordered list of data."
ListSource.display = "hide"
ListSource.path = "resources/sources/list_source.md"

TreeSource.description = "A data source describing an ordered hierarchical tree of data."
TreeSource.display = "hide"
TreeSource.path = "resources/sources/tree_source.md"

Validators.description = "Mechanisms for validating that input meets a given set of criteria."
Validators.display = "hide"

["Hardware"]
path = "hardware"

Camera.description = "A sensor that can capture photos and/or video."
Camera.display = "table"
Camera.unsupported = ["gtk", "winforms", "web", "textual"]

"Location services".description = "A sensor that can capture the geographical location of the device."
"Location services".path = "hardware/location.md"
"Location services".display = "table"
"Location services".unsupported = ["winforms", "web", "textual"]

Screen.description = "A representation of a screen attached to a device."
Screen.path = "hardware/screens.md"
Screen.display = "table"
Screen.beta = ["web", "textual"]

["Other"]
path = ""

Constants.description = "Symbolic constants used by various APIs."
Constants.display = "hide"

Keys.description = "Symbolic representation of keys used for keyboard shortcuts."
Keys.display = "hide"

Types.description = "Utility data structures used by Toga APIs."
Types.display = "hide"
